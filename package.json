{
  "name": "chrome-extension-usearly",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && mkdir -p dist/assets/icons dist/styles dist/popup && cp -r src/assets/icons/* dist/assets/icons/ && cp -r src/styles/* dist/styles/ && cp -r src/popup/* dist/popup/ && cp manifest.json dist/",
    "watch": "tsc --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^22.9.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.6.3",
    "webpack": "^5.96.1",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  }
}
